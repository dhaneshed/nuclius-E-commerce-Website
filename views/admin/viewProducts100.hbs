<section class="">
    <div class="container d-flex justify-content-center">
      <div class="row w-75 mt-5">
        <div class="col-md-12">
          <h2 class="text-center">Products</h2>
          <form
            action="/admin/add_sub"
            method="post"
            enctype="multipart/form-data"
          >
            
          </form>
        </div>
      </div>
    </div>
  </section>
  <!-- Table section -->
  <section  mx-auto>
    {{!-- <div class="card-body">
      <div class="row mt-5 mx-auto">
        <div class="table-responsive">
          <div class="table-responsive"> --}}
            <table class="table mt-5"  id="productsTable">
              <thead>
                <tr>
                  <th>No:</th>
                  <th>Product Name</th>
                  <th>Price</th>
                  <th>Category</th>
                  <th>Description</th>
                  <th>Quantity</th>
                  <th>Image</th>
                  <th>Options</th>
                </tr>
              </thead>
               <tbody>
        {{#each products}}
        <tr>
          <td>{{this.count}}</td>
          <td>{{this.Name}}</td>
          <td> ₹{{this.Price}}</td>
          <td>{{this.Category}}</td>
          <td>{{this.Description}}</td>
           <td>{{this.Quantity}}</td>
          <td><img class="zoomable-image" style="width:50px;height:50px;" src="/uploads/{{this.images.[0]}}" alt="">
          </td>
          <td>

            <a href="/admin/editProduct/{{this._id}}" class="btn btn-primary">Edit</a>
            <a href="/admin/deleteProduct/{{this._id}}" class="btn btn-danger"
              onclick=" return confirm('Do you want to delete {{this.Name}}?Are you sure?')">Delete</a>


          </td>
        </tr>
        {{/each}}
      </tbody>
            </table>
          {{!-- </div>
        </div> --}}
        <!-- table-responsive end// -->
      {{!-- </div>
    </div> --}}
  </section>
  